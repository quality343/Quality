# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local development database (project-local PostgreSQL data directory, no longer
# used by the app but the instance is kept until the Turso migration is signed off)
/.localdb/

# local SQLite/libSQL file database (TURSO_DATABASE_URL="file:./dev.db")
prisma/dev.db*
*.db-journal
*.db-wal
*.db-shm

# local tooling state and temp scratch files (preview logs, cookie jars, one-off scripts)
/.freebuff/
.tmp-*

# accidental Windows env-var expansion artifact
/%SystemDrive%/
